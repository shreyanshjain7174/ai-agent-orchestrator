name: Test Suite

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - feat/**

jobs:
  unit-tests:
    name: Unit Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest pytest-asyncio pytest-cov pytest-timeout pytest-mock

      - name: Syntax validation
        run: |
          python -m py_compile dynamic_orchestration.py mcp_server.py orchestrator.py autonomous_orchestrator.py

      - name: Run deterministic unit tests
        run: |
          python -m pytest tests/unit/test_dynamic_orchestration.py -q

      - name: Run deterministic failure-mode tests
        run: |
          python -m pytest tests/failure_modes/test_dynamic_planning_failure_modes.py -q
